laravel: "@testbench"

providers:
  - IGE\ChannelLister\ChannelListerServiceProvider

migrations:
  - workbench/database/migrations
  - database/migrations

seeders:
  - Workbench\Database\Seeders\DatabaseSeeder

# env:
#   - DB_CONNECTION=sqlite
#   - DB_DEFAULT_SEEDER=Workbench\Database\Seeders\DatabaseSeeder

factories:
  - database/factories

workbench:
  start: "/channel-lister"
  user: "channel@lister.com"
  install: true
  health: false
  # env: workbench/.env
  discovers:
    web: true
    api: true
    commands: true
    components: false
    factories: true
    views: true
  build:
    - asset-publish
    - create-sqlite-db
    - db-wipe
    - migrate-fresh
    - view-clear
    - asset-clear
  assets:
    - laravel-assets
  sync:
    - from: storage
      to: workbench/storage
      reverse: true
